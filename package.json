{
  "main": "src/index.js",
  "name": "demo",
  "version": "0.0.1",
  "scripts": {
    "test": "jest",
    "build": "esbuild --bundle --platform=node --target=node12 --outdir=./dist_npm src/index.js"
  },
  "devDependencies": {
    "esbuild": "^0.20.1",
    "jest": "^29.7.0"
  }
}
